# BoardStatusLED
Lightweight non-blocking status LED library for dual-core ESP32. Provides a unified interface for built-in or external LEDs (mono, RGB, WS2812). Fully non-blocking, compile-time configurable, and hardware independent.
